import torch
import os
from torchvision import datasets, transforms
from torch.utils.data import Dataset, DataLoader

class CacheData(Dataset):
    def __init__(self,):


    def __len__(self):


    def __getitem__(self, item):
